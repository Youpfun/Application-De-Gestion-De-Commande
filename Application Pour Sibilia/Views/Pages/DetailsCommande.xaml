<Page x:Class="Application_Pour_Sibilia.Views.Pages.DetailsCommande"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Application_Pour_Sibilia.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DetailsCommande">

    <Grid>
        <DataGrid x:Name="detailsCommande" ItemsSource="{Binding LesDetailsPlats }">
            <DataGrid.Columns>
                <DataGridTextColumn Header="NumCommande" Binding="{Binding NumCommande}"/>
                <DataGridTextColumn Header="Nomplat" Binding="{Binding NomPlat}"/>
                <DataGridTextColumn Header="Quantite" Binding="{Binding Quantite }"/>
                <DataGridTextColumn Header="Prix Unitaire" Binding="{Binding Prix ,StringFormat={}{0:N2}€} "/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Page>
