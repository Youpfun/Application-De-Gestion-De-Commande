<Page x:Class="Application_Pour_Sibilia.Views.Pages.GestionDesPlats"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Application_Pour_Sibilia.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <ui:Button Width="334" Height="40"
           FontSize="16" FontWeight="Bold"
           Background="#D0021B" Foreground="White"
           Click="CreePlat_Click" VerticalAlignment="Top" Margin="10,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ui:SymbolIcon Symbol="Add12" VerticalAlignment="Center"/>
                <TextBlock Text="   Ajouter un plat" Margin="0,0,8,0" VerticalAlignment="Center"/>
            </StackPanel>
        </ui:Button>

    </Grid>
</Page>
